# EnergyPlus Models

Please place your `.idf` (baseline building model) and `.epw` (weather data) files in this directory.

The simulation expects:
- `baseline.idf`
- `weather.epw`

If your files are named differently, please update the paths at the bottom of `backend/simulation.py`.
